apiVersion: v1
kind: Service
metadata:
  name: {{ include "sophia-memory-engine.fullname" . }}
  labels:
    {{- include "sophia-memory-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sophia-memory-engine.selectorLabels" . | nindent 4 }}
